# 📊 คู่มือการอ่านรายงานผลทดสอบ PPN GREAT API (HTML Test Report Guide)

คู่มือนี้จัดทำขึ้นเพื่อช่วยอธิบายวิธีการอ่านหน้าต่างรายงานสรุปการทดสอบระบบหลังบ้าน (HTML Interactive Dashboard) ที่สร้างขึ้นโดย Newman CLI หลังจากรันคำสั่งทดสอบครบถ้วน

---

## 🎨 1. แถบตั้งค่า และสถิติภาพรวม (Header & KPI Cards)

เมื่อคุณเปิดรายงานผ่านเบราว์เซอร์ คุณจะพบกับส่วนประเมินผลหลักดังนี้:

*   **ปุ่มเปลี่ยนธีม (Light/Dark Mode):** อยู่ด้านบนซ้ายสุด สามารถคลิกเพื่อสลับระหว่างธีมสว่างและธีมมืดเพื่อความสบายตา
*   **แถบฟิลเตอร์ (Tabs Filter):**
    *   `Summary`: แสดงผลรายงานสรุปตัวชี้วัด (KPI) ภาพรวมทั้งหมด
    *   `Total Requests (69)`: แสดงรายชื่อคำขอ API ทั้งหมด 69 ตัว พร้อมรายละเอียดหัวข้อย่อย
    *   `Failed Tests (0)`: แสดงรายการที่ยิงแล้วไม่ผ่าน (ในที่นี้ขึ้นเลข **0** แปลว่าผ่านหมดทุกเส้น)
*   **การ์ดสรุปผลลัพธ์หลัก (KPI Cards):**
    *   **TOTAL ASSERTIONS (90):** จำนวนจุดที่โปรแกรมเช็คตรวจสอบความถูกต้อง (เช่น การเช็คว่าโครงสร้างตรงไหม, ข้อมูลห้ามว่าง, รหัสสินค้าตรงเงื่อนไข) ทั้งหมด 90 จุด
    *   **TOTAL FAILED TESTS (0):** จำนวนจุดตรวจสอบที่ล้มเหลว (การเป็น **0** และมีสีเขียวแปลว่าผ่าน 100%)

---

## ⏱️ 2. ข้อมูลประสิทธิภาพเครือข่าย (Timings & Data)

แสดงผลข้อมูลประสิทธิภาพของเซิร์ฟเวอร์หลังบ้าน:

*   **Total run duration (23.8s):** เวลาที่ใช้ยิง API ครบทุกขั้นตอน ตั้งแต่ล็อกอิน ยื่นซัพพลายเออร์ จัดของคลัง จนถึงทำรายงานสรุป
*   **Average response time (263ms):** ความเร็วเฉลี่ยในการตอบสนองของเซิร์ฟเวอร์ต่อหนึ่งคำขอ (เฉลี่ย 0.26 วินาที ถือว่าอยู่ในเกณฑ์เร็วมากและตอบสนองได้ยอดเยี่ยม)

---

## 📂 3. การเจาะดูรายละเอียดทีละ API (Total Requests Tab)

หากคุณต้องการตรวจสอบการทำงานของฟังก์ชันใดฟังก์ชันหนึ่งโดยเฉพาะ:

1.  คลิกไปที่แท็บ **Total Requests (69)** ด้านบน
2.  คุณจะเห็นโฟลเดอร์เรียงลำดับตามกระบวนการทำงานจริง เช่น `Auth & Account`, `Customer`, `Project`, `Suppliers`, `Logistics`
3.  **รายละเอียดของแต่ละ API:**
    *   เมื่อคลิกเลือก API ตัวที่ต้องการ จะมีรายละเอียด **Request Method** (`GET`/`POST`/`PUT`/`PATCH`), **URL ปลายทาง**, **Headers** และ **Response Body (JSON)** ที่เซิร์ฟเวอร์ตอบกลับมาจริงให้วิเคราะห์ได้ทันที
    *   **Assertions List:** แสดงรายการหัวข้อตรวจสอบที่ระบุไว้ เช่น:
        *   `√ Status code is 200` (เซิร์ฟเวอร์ทำงานสำเร็จ)
        *   `√ Attempt count is 2` (จำนวนครั้งที่ยื่นตรวจตัวอย่างเพิ่มขึ้นจริง)
        *   `√ Status changed to Need Revision` (สถานะงานเปลี่ยนเป็นรอการแก้ไขตรงตามโฟลว์)

---

## 🚨 4. วิธีสังเกตจุดเมื่อเกิดข้อผิดพลาด (How to Debug Failures)

หากในอนาคตมีการแก้ไขโค้ดหลังบ้านแล้วรันสคริปต์นี้อีกครั้งแล้วพบว่า **Failed Tests** ไม่เป็น 0:

1.  กล่องการ์ด **TOTAL FAILED TESTS** จะเปลี่ยนเป็น **สีแดง** พร้อมระบุจำนวนจุดที่ไม่ผ่าน
2.  ให้คลิกที่แท็บ **Failed Tests** เพื่อตรวจสอบทันที
3.  ระบบจะชี้เป้าชัดเจนว่า:
    *   **เกิดที่ API เส้นไหน?** (เช่น `GET /api/reports/revenue-by-month`)
    *   **เงื่อนไขอะไรที่ไม่ผ่าน?** (เช่น *Expected response status code to be 200 but got 500* หรือ *Expected total to be 15000 but got 0*)
    *   ช่วยให้ทีมพัฒนาค้นพบจุดบั๊ก (Bug) และทำการแก้ไขได้ภายในไม่กี่นาที
